
import "example1.pkl"

local config: example1.GethConfig = new {
    chain = "Sepolia"
    metrics = true
    logLevel = "Info"
}

some = example1.create(example1.create2(config))

output {
  value = some
}
